{
  "name": "kiln-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm --filter kiln-compiler build",
    "check-types": "pnpm --filter kiln-compiler check-types",
    "test": "pnpm --filter kiln-compiler test"
  }
}