# Kiln Next.js Example

A minimal Next.js App Router application demonstrating native binary compilation with `kiln-compiler`.

## Build and Compile

```bash
# 1. Build Next.js standalone output and compile into binary
next build && kiln

# 2. Run the single-file executable
./server
```
